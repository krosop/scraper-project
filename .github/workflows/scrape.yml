name: Daily Scrape + Clean Pipeline (Python + Node.js)

on:
  schedule:
    - cron: "0 0 * * *"  # Every day at midnight UTC
  workflow_dispatch:

jobs:
  scrape:
    name: Scrape 8 Algerian Stores (Ouedkniss runs locally)
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      # ─── Node.js Setup ───
      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Node.js dependencies
        run: npm install --legacy-peer-deps

      # ─── Python Setup ───
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          cd scripts/pc-parts-scraper
          pip install -r requirements.txt

      # ─── Run Scraper V2 (8 non-Ouedkniss stores) ───
      # Ouedkniss is scraped locally and committed separately
      - name: Run Scraper V2
        run: cd scripts && python adapter_v2.py
        env:
          PYTHONIOENCODING: utf-8

      # ─── Convert data for new frontend (with taxonomy) ───
      - name: Convert data to DZ TechHunt format
        run: node scripts/convert-data-enhanced.js

      # ─── Upload artifacts ───
      - name: Upload results artifacts
        uses: actions/upload-artifact@v4
        with:
          name: scrape-results
          path: |
            public/clean-products.json
            public/data/products.json
          retention-days: 7

      # ─── Commit updated data ───
      - name: Commit clean data to repo
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          git add public/clean-products.json public/data/products.json
          git diff --cached --quiet || git commit -m "data: update scraped products $(date -u +%Y-%m-%d)"
          git push

  cleanup:
    name: Cleanup Expired Data
    runs-on: ubuntu-latest
    needs: scrape
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Node.js dependencies
        run: npm install --legacy-peer-deps
      - name: Run cleanup
        run: node scripts/cleanup.cjs
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}